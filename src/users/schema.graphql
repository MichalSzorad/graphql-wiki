type User {
  _id: String
  displayName: String
  email: String
  password: String
}

type Query {
  user(id: String!): User
}

type Mutation {
  addUser(email: String!, password: String!, displayName: String!): User
}
